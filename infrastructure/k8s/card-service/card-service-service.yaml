apiVersion: v1
kind: Service
metadata:
  name: card-service
  namespace: cba-platform
  labels:
    app: card-service
spec:
  type: ClusterIP
  selector:
    app: card-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081
