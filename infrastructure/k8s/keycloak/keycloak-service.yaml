apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: cba-platform
  labels:
    app: keycloak
spec:
  type: ClusterIP
  selector:
    app: keycloak
  ports:
    - name: http
      port: 8180
      targetPort: 8180
