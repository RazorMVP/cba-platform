package com.cba.product;

public enum InterestType {
    FLAT,               // Interest charged on original principal for full term
    DECLINING_BALANCE   // Interest charged on outstanding balance (reducing balance)
}
